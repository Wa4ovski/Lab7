package common.commands;

public enum CommandType {
    HELP,
    INFO,
    SHOW,
    ADD,
    UPDATE_ID,
    REMOVE_BY_ID,
    CLEAR,
    //No save
    EXECUTE_SCRIPT,
    EXIT,
    //add_if_min
    //add_if_max
    REMOVE_LOWER,
    SUM_OF_SALARY,
    COUNT_LESS_THAN_END_DATE,
    PRINT_FIELD_ASCENDING_PERSON
}
